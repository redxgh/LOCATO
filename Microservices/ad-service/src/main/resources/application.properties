spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=ads
spring.cloud.discovery.enabled=true
server.port=8081
spring.application.name=ad-service
spring.data.mongodb.autoIndexCreation=true
spring.servlet.multipart.max-file-size = -1
spring.servlet.multipart.max-request-size = -1


spring.security.oauth2.resourceserver.jwt.issuer-uri=http://localhost:8080/realms/locato
#when we send http request he sends the header authorization if exist he sends jwt he receives the public key from certs
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=http://localhost:8080/locato/protocol/openid-connect/certs

